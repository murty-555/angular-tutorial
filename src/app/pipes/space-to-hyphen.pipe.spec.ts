import { SpaceToHyphenPipe } from './space-to-hyphen.pipe';

describe('SpaceToHyphenPipe', () => {
  it('create an instance', () => {
    const pipe = new SpaceToHyphenPipe();
    expect(pipe).toBeTruthy();
  });
});
